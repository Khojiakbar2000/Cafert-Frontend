<!DOCTYPE html>
<html lang="en">
<head>
    <title>Cafert Admin - Sign Up</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --cafert-primary: #8B4513;
            --cafert-secondary: #D2691E;
            --cafert-accent: #FFD700;
            --cafert-dark: #2C1810;
            --cafert-light: #FFF8F0;
            --cafert-text: #2C1810;
            --cafert-text-light: #6B4423;
            --cafert-border: #E8D5C4;
            --cafert-gradient: linear-gradient(135deg, #8B4513 0%, #D2691E 50%, #CD853F 100%);
            --cafert-shadow: 0 8px 32px rgba(139, 69, 19, 0.15);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, var(--cafert-light) 0%, #FDF6F0 50%, #FFF8F0 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .signup-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            box-shadow: var(--cafert-shadow);
            border: 1px solid rgba(139, 69, 19, 0.1);
            padding: 3rem 2.5rem;
            width: 100%;
            max-width: 450px;
            backdrop-filter: blur(10px);
            position: relative;
            overflow: hidden;
        }
        
        .signup-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--cafert-gradient);
        }
        
        .signup-header {
            text-align: center;
            margin-bottom: 2.5rem;
        }
        
        .coffee-icon {
            font-size: 3rem;
            color: var(--cafert-primary);
            margin-bottom: 1rem;
            animation: pulse 2s ease-in-out infinite;
        }
        
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }
        
        .signup-title {
            font-family: 'Playfair Display', serif;
            font-weight: 700;
            color: var(--cafert-primary);
            font-size: 2rem;
            margin-bottom: 0.5rem;
        }
        
        .signup-subtitle {
            color: var(--cafert-text-light);
            font-size: 1.1rem;
            margin: 0;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
        }
        
        .form-label {
            display: block;
            font-weight: 600;
            color: var(--cafert-text);
            margin-bottom: 0.75rem;
            font-size: 1rem;
        }
        
        .form-control {
            width: 100%;
            padding: 1rem 1.25rem;
            border: 2px solid var(--cafert-border);
            border-radius: 12px;
            font-size: 1rem;
            background: rgba(255, 255, 255, 0.9);
            transition: all 0.3s ease;
            backdrop-filter: blur(5px);
        }
        
        .form-control:focus {
            outline: none;
            border-color: var(--cafert-primary);
            box-shadow: 0 0 0 0.25rem rgba(139, 69, 19, 0.15);
            transform: translateY(-2px);
        }
        
        .btn-signup {
            width: 100%;
            background: var(--cafert-gradient);
            color: white;
            font-weight: 600;
            padding: 1rem 2rem;
            border: none;
            border-radius: 12px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(139, 69, 19, 0.3);
            margin-top: 1rem;
        }
        
        .btn-signup::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }
        
        .btn-signup:hover::before {
            left: 100%;
        }
        
        .btn-signup:hover {
            transform: translateY(-3px) scale(1.02);
            box-shadow: 0 12px 35px rgba(139, 69, 19, 0.4);
        }
        
        .signup-footer {
            text-align: center;
            margin-top: 2rem;
            color: var(--cafert-text-light);
            font-size: 1rem;
        }
        
        .signup-footer a {
            color: var(--cafert-primary);
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }
        
        .signup-footer a:hover {
            color: var(--cafert-secondary);
            text-decoration: underline;
        }
        
        .form-icon {
            margin-right: 0.5rem;
            color: var(--cafert-primary);
        }
        
        @media (max-width: 768px) {
            .signup-container {
                padding: 2rem 1.5rem;
                margin: 1rem;
            }
            
            .signup-title {
                font-size: 1.75rem;
            }
            
            .coffee-icon {
                font-size: 2.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="signup-container">
        <div class="signup-header">
            <div class="coffee-icon">
                <i class="fas fa-coffee"></i>
            </div>
            <h2 class="signup-title">Create Your Account</h2>
            <p class="signup-subtitle">Join the Cafert Admin Panel</p>
        </div>
        
        <form action="/admin/signup" method="POST" id="signupForm">
            <div class="form-group">
                <label for="memberNick" class="form-label">
                    <i class="fas fa-user-tag form-icon"></i>Nickname
                </label>
                <input 
                    type="text" 
                    class="form-control" 
                    id="memberNick" 
                    name="memberNick" 
                    placeholder="Enter your nickname"
                    required
                >
            </div>

            <div class="form-group">
                <label for="memberPhone" class="form-label">
                    <i class="fas fa-phone form-icon"></i>Phone Number
                </label>
                <input 
                    type="text" 
                    class="form-control" 
                    id="memberPhone" 
                    name="memberPhone" 
                    placeholder="Enter your phone number"
                    required
                >
            </div>

            <div class="form-group">
                <label for="memberPassword" class="form-label">
                    <i class="fas fa-lock form-icon"></i>Password
                </label>
                <input 
                    type="password" 
                    class="form-control" 
                    id="memberPassword" 
                    name="memberPassword" 
                    placeholder="Enter your password"
                    required
                    minlength="6"
                >
            </div>

            <button type="submit" class="btn-signup">
                <i class="fas fa-user-plus"></i> Create Account
            </button>
        </form>
        
        <div class="signup-footer">
            <p>Already have an account? <a href="/admin/login">Login here</a></p>
        </div>
    </div>
</body>
</html> 